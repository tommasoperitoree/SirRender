`PFM` format $\to$ we need functions to read and write these files:

- Read $\Rightarrow$ take a `.pfm` file and extract from it the info needed to make an element `img` of the `HDRImage` class, i.e. where each  